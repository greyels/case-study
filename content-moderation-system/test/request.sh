#!/usr/bin/env bash
curl -X POST -H "Content-Type: text/plain" --data "The following example demonstrates using the DetectKeyPhrases operation with the AWS CLI. You must specify the language of the input text hell." $MODERATE_TEXT_URL
